# Loaded by `npm run dev` (Vite's development mode).
# Pairs with the backend's `make dev` on :8000.
# To point dev at something else, create .env.local — it wins and is git-ignored.
VITE_API_URL=http://localhost:8000
